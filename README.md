
# Business Card OCR
# Contents
# Usage
```
usage: __main__.py [-h] (-i INPUT | -f FILE | --test | filename)

Business Card OCR

positional arguments:
  filename

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Pass the input directly as text
  -f FILE, --file FILE  Pass the input as a file
```